import type { MenuItemType } from "$lib/zod/models/menu.model";

class MenuController{
    async createMenuItem(menuItem: MenuItemType){
        // checkout the service files and create a menu item service but name that file as menu.service.ts since we dont need two seperate files for menu and menu items but we do need them in two seperate tables and hence two seperate classes? or maybe a single classs yh singleclass mathy ig idk noooo 2 seperate classes since nale nammak crud stuff extend cheiyane reeti aakanee...


        // any doubts??
        //vayikate

        //👌👍
    }
}
export const menuController=new MenuController();